# VisionLog 产品需求文档（PRD）

> 文档状态：产品决策确认稿  
> 产品阶段：MVP / 自用验证  
> 更新日期：2026-08-17  
> 决策范围：已完成 13 轮产品压力测试并确认共同理解；技术选型另见 `TECHNICAL-DESIGN-TODO.md`

## 1. 产品概述

### 1.1 产品定位

VisionLog 是一款面向个人手机用户的照片日志产品。系统从用户指定的 Android 设备图片文件夹自动或手动同步照片，在服务端结合文件信息、EXIF 元数据和视觉模型识别结果，为每张照片生成结构化 Photo Log，再基于一个或多个 Photo Log 生成按日期、主题或二者组合组织的图文 Plog。

产品的首要价值是自动生成值得回顾的个人生活日志。照片搜索是支撑能力，服务端压缩图是长期可查看的独立副本，但不是原图备份，也不提供高可靠恢复承诺。

首期优先支持 Android，并提供 Web 端用于开发调试和跨设备访问。产品当前为自用项目，不包含订阅、计费和商业运营能力。

### 1.2 产品目标

- 减少个人用户整理手机照片的时间成本。
- 在照片同步后生成可追踪、可修正的结构化 Photo Log。
- 自动生成具有标题、叙事文字和图片编排的 Plog 草稿。
- 支持按日期、动态主题及组合条件回顾照片。
- 支持 Android 与 Web 端浏览、搜索、编辑、归档和清理。
- 保持自动识别、用户修正与历史版本之间的边界清晰。

### 1.3 非目标

MVP 不包含：

- 专业图片编辑、公开发布、社交社区和多人协作相册。
- iOS 原生客户端。
- 人脸身份识别和人物身份库；该能力列入二期。
- 视频及 Motion Photo 内容处理。
- 原图云备份或服务端高可靠备份承诺。
- 跟踪手机端照片编辑或替换后的新版本。
- 模型升级后自动重跑历史照片。
- 商业化、订阅、套餐和计费能力。

## 2. 目标用户与核心场景

### 2.1 目标用户

首期用户为产品所有者本人，主要使用 Android 手机，通过家庭局域网部署服务端，并通过 Tailscale 从其他设备访问。

数据模型保留 `user_id` 和用户隔离边界，但产品界面和运行方式按单用户设计。

### 2.2 核心场景

1. 用户选择一个 Android 设备图片文件夹作为同步目标。
2. Android 在系统允许的后台窗口内发现并上传新增照片；用户也可以手动同步。
3. 服务端解析文件信息和 EXIF，生成压缩主图和缩略图。
4. 服务端通过可替换的视觉模型 Provider 调用 Gemini API，为单张照片生成结构化结果。
5. 系统生成 Photo Log，并按日期及动态主题组织照片。
6. 服务端每日生成 Plog 草稿，用户在照片首页发现、审核和确认。
7. 用户通过 Android 或 Web 查看、搜索、修正、归档和删除内容。

## 3. 核心领域模型

### 3.1 PhotoAsset

PhotoAsset 表示一份去重后的照片内容及其服务端压缩副本。同一内容哈希对应一个 PhotoAsset，可以关联多个手机来源。文件名或路径变化不产生新资产；内容哈希不同的文件才视为不同资产。

主要信息包括：

- 内容哈希、感知哈希、格式、尺寸和方向。
- 服务端压缩主图、缩略图及压缩参数。
- 一个或多个设备来源，包括设备、MediaStore URI、相对路径和首次发现时间。
- 临时原图处理和清理状态。

### 3.2 Photo Log

Photo Log 是单张 PhotoAsset 对应的结构化日志，是系统最小内容单位。

数据分为三层：

1. 原始事实层：文件信息、服务端解析的 EXIF、来源与时间信息。
2. 模型推断层：标题、描述、标签、候选主题、场景及置信度。
3. 用户覆盖层：用户修正的日期、地点、描述、标签、备注和主题归属。

最终展示值按照“用户覆盖 > 模型推断 > 原始事实”的优先级合成。模型结果保持只读。用户修改任一字段后，整条 Photo Log 进入人工锁定状态，不再接受模型更新。

用户可以执行两种明确操作：

- 解除人工锁定：允许产生新的模型层，既有用户覆盖继续优先生效。
- 恢复为模型结果：清除全部用户覆盖并解除锁定，必须二次确认。

Web 与 Android 保存内容时使用版本号乐观锁。版本冲突时展示差异；不同字段可辅助合并，同一字段必须由用户选择，不允许静默覆盖。

| 信息类型 | 字段示例 |
| --- | --- |
| 标识 | Photo Log ID、PhotoAsset ID、用户 ID |
| 文件 | 文件名、格式、尺寸、宽高、方向、哈希 |
| 时间 | 拍摄时间、文件时间、发现时间、同步时间、发现时区 |
| 地点 | EXIF GPS、地点名称、地点来源 |
| 设备 | 手机型号、镜头和拍摄参数 |
| 视觉 | 场景、物体、人物数量、动物、食物、建筑 |
| 内容 | 标题、摘要、自然语言描述、内容标签 |
| 主题 | 候选主题、已确认主题、置信度 |
| 状态 | 待同步、处理中、待识别、已完成、失败、已归档 |
| 用户 | 覆盖字段、备注、反馈 |
| 模型 | Provider、模型 ID、Schema 版本、提示词版本、处理时间 |

### 3.3 Topic

Topic 是跨时间持续存在的照片分类，例如“猫”“城市夜景”“家庭聚餐”。Topic 本身不是 Plog，也不承载无限增长的叙事正文。

- LLM 可以自动创建高置信度、高频主题。
- 其他主题以候选形式出现。
- 用户确认候选时同时确认主题名称和候选照片列表。
- 近义主题仅提示合并，不允许模型静默合并。
- 用户可以创建、重命名、合并和删除主题。
- 归档 Topic 后停止自动匹配和生成新主题 Plog，历史关系和 Plog 保留。
- 删除 Topic 只删除分类及当前归属；既有 Plog 保留主题名称快照并标记来源已删除。

### 3.4 Plog

Plog 是基于一个或多个 Photo Log 生成的版本化照片叙事。Plog 可以由日期、主题或日期与主题组合定义，也允许仅包含一张照片。

Plog 保存：

- 生成规则：日期、主题、手工选图或组合条件。
- 成员照片快照：生成当时纳入的完整 Photo Log 清单。
- 正文引用照片：从成员照片中精选、实际出现在图文正文里的照片。
- 标题、开场摘要和多个图文段落。
- 生成时区、输出语言和模型元信息。
- 草稿、已确认、待更新等状态。
- 当前版本和上一个版本。
- 每个正文段落引用支撑它的 `photo_log_id` 和使用的事实字段。

Plog 文案采用编辑化图文故事风格，可以保守表达氛围，但不得虚构人物身份、关系、地点或事件。用户修改 Photo Log、主题或成员后，已有 Plog 不自动覆盖，而是标记为“有可用更新”，生成候选新版供用户比较确认。

## 4. 产品范围与优先级

### 4.1 MVP（P0）

- Web 手动上传到正式数据体系。
- PhotoAsset、Photo Log、Topic 和 Plog 完整数据链路。
- Android 单目标文件夹自动同步和手动同步。
- 服务端异步 Job、失败重试和状态反馈。
- 文件及 EXIF 提取、WebP 压缩主图和缩略图。
- 可替换视觉模型 Provider，首选 Gemini API。
- 单图结构化识别和动态主题生成。
- 每日日期 Plog 草稿和手动生成 Plog。
- 照片首页、Plog 审核、关键词搜索和筛选。
- 归档、回收站和永久删除。
- Tailscale 网络访问。

### 4.2 MVP 后增强（P1）

- 相似照片检测、更完善的事件聚类和自然语言搜索。
- 地图和时间轴浏览。
- 多个目标文件夹和多 Android 设备。
- 从桌面文件夹批量导入。
- 更多 Plog 写作样式。

### 4.3 二期（P2）

- iOS 原生客户端。
- 人脸检测、人物聚类和用户确认的人物身份库。
- 视频与 Motion Photo 处理。
- 用户自定义识别规则。

## 5. 总体架构

### 5.1 架构原则

- Android 和 Web 负责视图、指令、同步配置和状态展示。
- 家庭服务端负责接收、解析、压缩、存储、Job 调度、模型调用、结构化、主题和检索。
- Android 自动同步保证最终一致；手动同步及前台处理满足准实时体验。
- 每个 Job 步骤独立、幂等、可重试。
- 视觉模型通过 Provider 接口接入，领域模型和任务协议不绑定 Gemini。
- 应用不实现登录；网络访问完全依赖 Tailscale 和家庭网络边界。

### 5.2 逻辑流程

```text
Android 发现或用户选择照片 / Web 手动上传
  -> 创建持久同步任务
  -> 上传至服务端临时区
  -> 完整性校验与内容哈希去重
  -> 解析文件及 EXIF 信息
  -> 修正方向并生成 2048px WebP 压缩主图
  -> 校验主图并生成缩略图
  -> 立即删除临时原图，24 小时兜底清理
  -> 单图调用视觉模型 Provider
  -> Schema 与业务规则校验
  -> 生成或更新 Photo Log 模型层
  -> 生成候选主题
  -> 每日或手动生成 Plog 草稿
  -> Android / Web 展示状态与结果
```

### 5.3 部署与模型地区边界

中国大陆不在 Gemini API 官方支持地区内，因此不得把代理访问视为正式架构保证。

- MVP 采用 Provider 适配层，开发阶段优先实现 Gemini Provider。
- 正式部署前确认 Provider 的地区可用性、条款和网络路径。
- 必要时可将模型 Worker 部署在官方支持地区，或替换为可正式使用的视觉模型供应商。
- Provider 必须输出统一的版本化结构化 Schema。

## 6. Android 同步需求

### 6.1 目标文件夹

- MVP 支持一个 MediaStore 实体图片文件夹。
- 内部以存储卷和 MediaStore 相对路径标识，不承诺支持系统相册中的虚拟集合。
- 自动递归处理目标文件夹的子目录。
- 首次启用时展示既有照片数量，并允许：全部导入、按日期导入、仅处理后续新增。
- 一张旧照片在启用后首次进入目标文件夹时，也视为新增并处理。
- 移除目标文件夹仅停止后续发现，保留服务端 Photo Log 和压缩图。

### 6.2 权限

- 支持完整照片权限、部分照片权限和未授权三种状态展示。
- Android 14+ 仅授予部分照片访问时，禁用文件夹自动同步并引导授予完整权限。
- 部分权限下仍允许手动导入已授权照片。
- GPS 媒体位置权限被拒绝时照常处理，但地点字段可以为空。
- Gemini 的视觉猜测不得写入 GPS 或确定地点字段。

### 6.3 发现与增量同步

- 开启时记录文件夹 checkpoint。
- 使用稳定资源标识、内容哈希、首次发现时间和补偿扫描保证幂等。
- Android 前台时快速发现；后台使用系统任务能力最终同步。
- 用户打开 App 时立即执行补偿扫描。
- 手机端照片同步完成后被移动或删除，不影响服务端长期副本。
- 已同步资源在手机端被编辑或替换时忽略变化，不创建新版本，也不覆盖服务端内容。
- 同一 MediaStore 资源只改变文件夹路径时更新 PhotoSource，不重新上传或识别。
- 仅在原始文件字节哈希完全一致时自动合并资产；像素或感知哈希相似只作重复候选提示。
- 永久删除照片后保存最小“忽略此来源”标记，阻止仍在目标文件夹内的文件被再次同步。标记不得包含图片、描述或识别内容。

### 6.4 网络、离线与重试

- 自动同步默认允许 Wi-Fi 和移动网络。
- 用户可以改为仅非计费网络或增加充电条件。
- 家庭服务器不可达时，Android 持久保存资源 URI、指纹、发现时间和状态，不复制原图。
- 服务器恢复后自动重试，补偿扫描负责修复遗漏。
- 临时错误指数退避，默认最多自动重试 5 次。
- 永久错误或达到上限后进入“需要处理”，用户可以重试或忽略。

### 6.5 同步时效

- 自动同步是最终一致能力，不承诺发现后 5 分钟内完成。
- 系统允许后台执行且网络满足时尽快同步。
- 试运行阶段暂以 P90 15–30 分钟作为评估目标，不作为硬 SLA。
- 手动同步和 App 前台发现后，以 5 分钟内出现基础 Photo Log 为产品目标。

### 6.6 时间事实

- 日期组织优先级为：用户修正 → EXIF 拍摄时间 → MediaStore 拍摄时间 → 文件创建时间 → 首次发现时间。
- 保存所有原始时间值及最终采用依据。
- Web 上传时，EXIF 含时区偏移则采用该偏移；否则采用用户配置的默认时区。

## 7. Web 上传与跨设备访问

### 7.1 Web 上传

- Web 是第一条交付链路：手动上传 → 解析压缩 → 单图识别 → Photo Log → Plog。
- Web 上传进入正式数据体系，与 Android 上传使用相同 Job 和数据模型。
- 支持单张和多张上传、进度、失败状态及重试。

### 7.2 访问控制

- 应用本身不提供用户名、密码或 Passkey 登录。
- Tailnet 内任何设备均可查看、编辑、归档、删除和永久删除全部内容。
- 数据模型保留固定单用户 `user_id`，为未来权限扩展留出边界。
- 部署说明必须明确：Tailscale 网络成员等同于 VisionLog 完整权限用户。

## 8. 服务端 Job

### 8.1 Job 状态

- 待上传、上传中、排队中、校验中、解析中、压缩中。
- 识别中、整理中、已完成、部分完成／待识别。
- 需要处理、已取消。

### 8.2 处理要求

- 每个任务具有唯一 ID、当前步骤、进度、错误和时间戳。
- 每个步骤可独立重试且保持幂等。
- 模型不可用时仍生成只有文件及 EXIF 的 Photo Log，并标记“待识别”。
- 失败不得删除已成功生成的压缩图或 Photo Log。
- 用户手动触发的少量照片优先于后台历史导入。
- 模型升级不自动重识别历史照片，也不生成批量升级提示。
- 用户仍可对明确选择的单张内容执行手动重新识别。
- 用户删除处理中的资产时立即标记删除，取消尚可取消的任务；运行中步骤结束后不得进入下游步骤。
- 用户取消任务时停止后续处理，并自动保留已经成功产生的压缩图和 Photo Log。未识别的部分结果标记“处理已取消”，不参与自动 Plog，用户可手动继续。
- 服务端存储不足时停止接收新上传，客户端继续保留待同步队列，并在应用内显示严重故障横幅；不得自动删除或静默降低图片质量。

## 9. 图片处理与存储

### 9.1 临时原图

- 原图仅在服务端临时区存在，用于校验、EXIF 解析、方向修正和压缩。
- 压缩主图校验成功后立即删除原图。
- 无论任务状态如何，原图最迟在上传后 24 小时由兜底任务清理。

### 9.2 长期图片

- 长期保存一张压缩主图和一张列表缩略图。
- 主图默认最长边 2,048 px、WebP 格式，并修正 EXIF Orientation。
- 缩略图是可重建缓存，不是独立资产。
- 压缩图默认永久保留，即保留到用户主动删除相关内容。
- 服务端不跟随手机源照片的移动、删除或编辑而改变既有副本。
- MVP 支持 JPEG、PNG、WebP、HEIC/HEIF；其他格式标记为不支持。GIF、SVG、RAW 和视频不在 MVP 范围内。
- 异常大图片由 Android 端生成受控尺寸上传副本，并保留必要元数据；服务端仍限制文件大小、像素数和解码资源。

### 9.3 可靠性边界

- VisionLog 不提供服务器数据备份和恢复能力。
- 手机原图和服务器压缩副本同时丢失时，数据不可恢复。
- 产品不主动展示未备份风险提示。

## 10. 视觉模型与结构化识别

### 10.1 Provider 契约

视觉 Provider 需要输出：场景、主要物体、人物数量、动植物、食物、建筑、照片标题、摘要、自然语言描述、候选主题、敏感内容分类、置信度，以及模型、Schema 和提示词版本。

Photo Log 只有在获得标题、摘要、场景、至少一个标签和敏感分类后才标记为“识别完成”。Schema 合法但缺少这些内容时保持“待识别”或“部分完成”。

所有推断字段允许为空，未知事实使用 `null/unknown`，不得强行选择概率最高的答案。每个推断字段分别保存置信度；Provider 无法给出可信数值时映射为高／中／低／未知。每个最终字段记录来源类型：EXIF、文件、视觉模型或用户修正，不要求模型输出内部思维过程。

核心场景、敏感类别和媒体类型采用受控枚举，细节使用自由标签。服务端必须在模型返回后执行 Schema 和业务规则校验。

### 10.2 Gemini 实现决策

- Photo Log 每张照片独立调用一次，确保失败隔离和幂等。
- 模型输入使用服务端最终保存的压缩主图。
- MVP 使用 HIGH 媒体分辨率作为质量基线，评测后再决定是否降低。
- 仅把经过校验、与叙事相关的时间和地点元数据作为文本上下文传给模型。
- Gemini 不作为 EXIF 读取来源，也不得覆盖服务端解析的时间和 GPS。
- API 密钥只保存在服务端。
- 使用明确版本的稳定模型 ID，不使用 `latest` 或实验别名作为生产配置。

### 10.3 免费服务与用户同意

- 系统允许配置 Gemini 免费或付费服务。
- 检测到免费服务时，启用识别前明确警告：输入输出可能用于产品改进，并可能被人工审核。
- 用户确认风险后可以继续使用免费服务处理手动或自动同步照片。
- 同意状态、提示版本和确认时间需要记录。
- 用户可以随时暂停模型识别；暂停不影响照片同步和元数据 Photo Log。
- 撤回同意后停止新模型任务，并取消尚未发送的队列任务；既有结果保留，除非用户另行删除。
- 同意绑定 Provider、免费／付费状态及提示版本。条款或风险说明实质变化时暂停新任务，要求重新确认。
- Provider 切换不得静默发生。新 Provider 必须先展示数据条款并获得确认。
- 切换后历史识别结果保持原样，新 Provider 只处理新任务；每条结果记录实际 Provider。
- 没有可用或已授权 Provider 时继续同步、解析和压缩，建立“待识别”Photo Log，识别队列保持暂停。

### 10.4 原始响应与敏感内容

- 结构化识别结果和模型元信息长期保存。
- 模型原始响应保留 30 天后自动删除，包括 Schema 校验失败的响应。
- 敏感照片照常保存、识别和整理，首页及列表缩略图默认模糊。
- 用户可以关闭模糊或纠正分类；系统不得依据模型分类自动删除内容。
- 模糊偏好全局生效，任意 Tailnet 设备修改后影响所有设备。
- 支持批量标记“非敏感”，用户修正锁定整条 Photo Log，不被重新识别覆盖。

## 11. Plog 生成与管理

### 11.1 日期 Plog

- 默认每天生成前一天的日期 Plog 草稿。
- 当天只有一张照片时也生成日期 Plog，但不自动生成主题 Plog。
- 采用照片首次被发现时记录的手机时区进行归日。
- 服务端默认凌晨 03:00 执行，可在 Web 管理页修改全局触发时间。
- Web 提供“立即执行一次”。
- 服务器离线错过任务后，为每个遗漏日期分别补生成草稿。
- 重复触发时使用日期、成员快照、语言和提示词版本形成幂等键：输入未变化则返回既有草稿，有变化则生成候选新版。
- 每日任务最多等待照片识别 24 小时；仍有未识别照片时生成标记为“不完整”的草稿，识别补齐后提示更新。

### 11.2 主题与组合 Plog

- Topic 作为长期分类存在；主题 Plog 必须再选择有限日期范围或照片集合。
- LLM 可以提议有叙事价值的日期＋主题组合草稿，不穷举所有组合。
- 候选主题由用户同时确认名称和成员照片。
- 高置信度、高频主题可以自动创建；其他主题进入候选区。
- 自动创建阈值作为可评测配置，不向普通界面暴露，也不在 PRD 中写死具体数值。

### 11.3 手动生成

支持两种入口：在照片网格选择一组照片后生成 Plog；或者指定日期、主题或组合条件，由系统选出成员照片。

### 11.4 选图与叙事输入

- Plog 保存完整成员照片清单。
- 系统从成员中精选正文引用照片，未入选照片仍属于 Plog，并可在附件区查看。
- 生成 Plog 文案时不把图片再次发送给 Gemini，仅使用 Photo Log 的结构化文字数据。
- Plog 包含标题、开场摘要和多个文字段落，图片按段落关联混排。
- 默认写作风格为编辑化图文故事，允许保守氛围表达，不允许虚构事实。
- 默认输出简体中文；用户可以设置默认语言，并在单次生成时覆盖。
- 上下文过大时按时间或主题对全部 Photo Log 分批摘要，再用带照片 ID 的中间摘要生成最终 Plog，保留事实来源映射。
- 模型从正文引用照片中选择封面，用户可以更换。

### 11.5 草稿、确认与更新

- 自动生成内容先进入草稿箱，不直接成为正式 Plog。
- 产品内“发布”定义为“确认收录”，不生成公开链接。
- 未审核草稿永久保留，但旧草稿在界面中折叠。
- 照片或主题变化时，已有 Plog 标记“有可用更新”。
- 系统生成完整候选新版，用户比较后决定是否替换。
- 仅保留当前版本和上一个版本。
- 用户可以编辑标题、正文文字和封面，但不能调整段落结构、正文图片顺序或版式。
- 不发送系统通知，也不对旧草稿发送积压提醒；状态仅在用户打开应用后展示。

### 11.6 正确性反馈

每个 Plog 支持“可直接使用、选图有误、主题有误、文案有误”四类反馈，并允许附加备注。反馈用于评估自动整理质量，不自动触发内容修改。

## 12. 客户端体验

### 12.1 首页

- 首页主体为按拍摄时间排列的照片网格。
- 网格顶部展示“今日 Plog”和“待确认草稿”卡片。
- 敏感照片缩略图默认模糊。
- 同步异常和待处理任务以状态卡片展示。
- 照片按拍摄时间倒序并按日分组；缺少拍摄时间时按既定时间优先级回退。
- 存储不足、持续失败或 Provider 暂停时显示不可忽略的应用内横幅，但不发送系统通知。

### 12.2 Photo Log

展示压缩主图、拍摄时间、发现时区、地点、模型描述、主题标签、文件设备信息、来源、所属 Plog、处理状态及用户覆盖内容。用户可以编辑、重新识别、归档或删除。

照片有 GPS 但反向地理编码失败时，列表地点为空，详情页允许查看坐标；不得使用 Gemini 猜测结果替代地点事实。

### 12.3 Plog

展示标题、日期范围、主题、封面、图文正文、正文引用照片、完整成员、版本和待更新状态，并提供确认、编辑、反馈、归档和删除入口。

MVP 支持批量归档、删除、添加主题、生成 Plog 和重新识别，不支持批量修改日期或地点。

## 13. 搜索与筛选

MVP 支持按日期范围、主题、标签、地点、处理状态、文件类型和归档状态筛选；支持对 Photo Log 描述、用户备注、Topic 和 Plog 标题进行关键词搜索。结果按“照片／主题／Plog”分组，组内排序。自然语言语义搜索列入 P1。

## 14. 归档、删除与回收站

### 14.1 归档

- 归档内容默认不出现在首页，但仍可搜索和查看。
- 归档 Photo Log 不参与未来自动 Plog 生成，但允许用户手动选用。
- 归档不删除压缩图或结构化数据，并支持恢复。

### 14.2 删除

- 删除 Plog 默认只删除 Plog、文案、版本和成员关系，不删除底层照片。
- 同一 PhotoAsset 有多个来源时，删除界面必须区分“移除此来源”和“删除整张照片记录及全部来源”。
- 删除服务端内容不删除手机本地文件。
- 被已确认 Plog 引用的 Photo Log 删除后，Plog 保留不可见图片占位并标记引用已删除，不自动改写文案；回收站恢复后占位恢复。
- Plog 成员被永久删除后，旧版本保留文字和剩余图片，显示不可恢复的匿名占位并标记版本不完整。

### 14.3 回收站

- 删除内容进入回收站并保留 30 天，期间可以恢复。
- 用户可二次确认后立即永久删除。
- 到期或立即删除请求应在 24 小时内清除图片、结构化结果、关系和索引。
- 同一内容在回收站期间再次被同步时保持删除状态并提示用户，不自动恢复。用户恢复时合并到当前 PhotoAsset，并恢复旧用户修正、主题和 Plog 关系。
- 永久删除后 AuditLog 仅保留内部 ID、操作时间和结果，不保留文件名、描述、标签或可重建内容的信息。

### 14.4 清空全部数据

- 提供“一键清空”，要求输入确认短语，默认将全部内容作为一个批次放入回收站。
- 清空时为当前所有来源建立最小忽略标记，阻止后台立即重新同步。
- 用户可重置 checkpoint 并主动重新导入；立即永久清空需要额外确认。

## 15. 数据模型

| 实体 | 说明 |
| --- | --- |
| User | 固定单用户记录，保留未来隔离边界 |
| Device | Android 设备、时区和同步状态 |
| SourceFolder | MediaStore 目标路径、递归规则和 checkpoint |
| PhotoSource | 照片在某设备文件夹中的来源关联 |
| PhotoAsset | 内容去重后的压缩主图和缩略图 |
| SyncTask | Android/Web 上传批次及状态 |
| ProcessingJob | 服务端异步步骤、重试和错误 |
| MetadataRecord | 文件及 EXIF 原始事实 |
| RecognitionResult | Provider 原始及结构化识别结果 |
| UserOverride | 用户对模型或元数据的覆盖层 |
| PhotoLog | 单张照片的最终结构化日志 |
| Topic / TopicPhoto | 动态主题及照片关系 |
| Plog / PlogVersion | 生成规则、成员快照、正文和版本 |
| ArchiveRecord | 归档状态及恢复信息 |
| TrashRecord | 回收站、到期时间和删除状态 |
| ConsentRecord | 免费模型风险提示和用户确认 |
| AuditLog | 关键编辑和删除操作 |

### 15.1 数据导出、恢复与索引

- 支持手动生成完整导出包，包含压缩图、Photo Log、Topic、Plog、关系、用户修正和版本化 manifest。
- 模型原始响应默认不导出；用户可显式勾选“包含调试数据”。
- 导出包含内容和非秘密配置，但不包含 API 密钥、会话、设备 URI、忽略来源或模型同意授权。
- MVP 只允许把导出包导入空白实例。导入后校验 manifest、文件哈希、实体数量和引用完整性。
- 不提供定时导出，手动导出属于可迁移性而非备份承诺。
- 搜索索引是可重建派生数据，必须能从 Photo Log、Topic 和 Plog 全量重建，且不调用视觉模型。

## 16. 非功能需求

### 16.1 性能与可靠性

- Android/Web 首页目标加载时间不超过 2 秒。
- 网格优先加载缩略图，主图按需加载。
- 单次历史导入至少支持 1,000 张照片。
- Android 队列和服务端 Job 持久化；上传支持断点续传。
- 服务重启后任务可恢复，重复上传不产生重复 PhotoAsset 或 Photo Log。
- 模型故障不影响元数据 Photo Log 建立。
- 不对服务端磁盘故障提供备份恢复保证。
- Web 与 Android 修改内容使用乐观锁，冲突不得静默覆盖。

### 16.2 隐私与安全

- 家庭网络及 Tailnet 内使用加密传输。
- 模型 API 密钥和服务凭据不得进入客户端或代码仓库。
- 临时原图 24 小时内强制清理。
- 明确披露第三方模型处理、免费服务数据使用风险及地区限制。
- 免费服务必须获得版本化的明确同意。
- Tailnet 中所有设备具有完整读写及永久删除权限。
- 无系统通知；严重故障通过应用内状态卡片或横幅呈现。

### 16.3 可观测性

监控后台发现到上传、建档和识别的 P50/P90/P99 时间，上传/解析/压缩/识别成功率，队列积压、重试次数、临时原图超期数量、Provider 错误和调用量、主题纠正比例，以及 Plog 草稿确认率和错误类型。

## 17. MVP 验收标准

- Web 上传可以跑通正式 Photo Log/Plog 链路。
- Android 可以选择一个 MediaStore 实体图片文件夹并递归发现图片。
- 首次启用支持全量、按日期或仅新增三种导入方式。
- 部分照片权限下自动同步被禁用，手动导入仍可用。
- 服务器离线期间任务不丢失，恢复后继续同步。
- 手机源照片移动、删除或编辑不改变已同步内容。
- 服务端解析 EXIF，生成 2,048 px WebP 主图和缩略图。
- 临时原图成功后立即删除，并有 24 小时兜底清理。
- 单张照片通过 Provider 独立识别并通过版本化 Schema 校验。
- 模型失败时仍建立“待识别”的元数据 Photo Log。
- 每天按照片发现时区生成前一天的日期 Plog 草稿。
- 单张照片可以生成日期 Plog，但不自动生成主题 Plog。
- Plog 仅使用 Photo Log 结构化文字生成，不再次发送图片。
- 用户可以确认主题名称及成员、审核 Plog、比较候选新版。
- 首页以照片网格为主体，并展示 Plog 和待审核入口。
- 敏感缩略图默认模糊。
- 归档照片不参与自动 Plog，但可手工选用。
- 删除 Plog 不删除照片；删除照片时区分来源与整个资产。
- 回收站保留 30 天，永久清理 SLA 为 24 小时。
- Tailnet 中任意设备无需登录即可完成全部读写操作。
- 每字段保存置信度和来源，未知事实不会被强行填充。
- Plog 段落能够追溯到支撑它的 Photo Log 和事实字段。
- Provider 授权撤回、条款变化和切换均正确暂停或隔离任务。
- 多端并发编辑发生冲突时不会静默丢失数据。
- 处理中删除、任务取消、回收站恢复和永久删除符合状态机规则。
- 完整导出包可在空白实例通过校验后恢复，搜索索引可独立重建。
- 一键清空不会被后台同步立即逆转。

## 18. 成功指标

连续使用 30 天，并满足：

- 至少 80% 的 Plog 被标记为“可直接使用”。
- 用户每周至少主动回顾一次 Plog。
- 自动同步没有无法通过补偿扫描恢复的照片遗漏。
- 临时原图超期残留数量为 0；出现任何超期残留均阻止 MVP 验收。

同时记录首次同步完成率、同步/压缩/识别成功率、重复资产率、Plog 各类错误比例、Photo Log 修正率、草稿确认率和平均确认时延。

## 19. 已确认决策摘要

| 决策域 | 结论 |
| --- | --- |
| 核心价值 | 自动生成可回顾的生活日志 |
| 最小单位 | 单张照片的 Photo Log |
| Plog | 基于一张或多张 Photo Log 的图文叙事 |
| Plog 模型 | 日期/主题/组合规则＋成员快照＋版本 |
| 首期平台 | Android 优先，Web 先验证完整链路 |
| 同步源 | 单个 MediaStore 图片文件夹，递归子目录 |
| 同步时效 | 自动最终一致；手动及前台准实时 |
| 手机变更 | 移动、删除和编辑均不改变服务端内容 |
| 首页 | 照片网格，顶部提供 Plog 入口 |
| 服务端图片 | 2,048 px WebP 主图＋缩略图 |
| 原图 | 压缩成功立即删除，24 小时兜底 |
| 备份 | 不提供，不展示风险提示 |
| 视觉模型 | Provider 适配层，首选 Gemini |
| Gemini 免费服务 | 警告后允许用户选择使用 |
| 单图识别 | 一图一请求、HIGH 分辨率基线 |
| Plog 模型输入 | 仅 Photo Log 结构化文字，不再次发送图片 |
| 动态主题 | 高置信度自动创建，其余确认名称和成员 |
| 每日生成 | 次日凌晨生成草稿，可配置和立即执行 |
| 单图 Plog | 允许日期 Plog，不自动生成主题 Plog |
| Plog 版本 | 当前版本＋上一个版本 |
| 敏感内容 | 正常处理，列表缩略图默认模糊 |
| 访问控制 | 无应用登录；Tailnet 任意设备完整权限 |
| 归档 | 不参与自动生成，可用于手动 Plog |
| 回收站 | 30 天，永久清理 SLA 24 小时 |
| 视频 | MVP 忽略 |
| 商业模式 | 自用，不考虑商业运营 |
| 人工修正 | 修改任一字段后锁定整条 Photo Log |
| 并发编辑 | 版本号乐观锁，冲突由用户处理 |
| Provider 容灾 | 不自动切换；新 Provider 需重新确认 |
| Plog 事实 | 段落引用 Photo Log 和事实字段 |
| Plog 编辑 | 可改文字和封面，不可调整正文布局 |
| 系统通知 | 不发送；严重问题使用应用内横幅 |
| 数据迁移 | 手动完整导出，只能导入空白实例 |
| 永久删除防回流 | 保存最小忽略来源标记 |

## 20. 产品决策状态

本 PRD 的产品决策树已完成 13 轮压力测试，前沿为空，并由用户确认已达成共同理解。

数据库、编程语言、任务队列、对象存储、部署容器、监控组件等实现选择不属于本 PRD。相关约束与待选事项记录在 `TECHNICAL-DESIGN-TODO.md`，后续通过独立技术设计流程确定。
