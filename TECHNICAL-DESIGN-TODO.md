# VisionLog 技术设计待办

> 状态：MVP 已实现；本文保留为原始问题清单  
> 来源：`PRD.md` 已确认产品决策  
> 原则：本文只记录约束、验证项和待选问题，不预先指定技术栈。

实现后的技术选择与 seam 见 `TECHNICAL-DESIGN.md`，接口见 `API.md`，运行与验收分别见 `OPERATIONS.md`、`ACCEPTANCE.md`。本文件不再代表未完成工作。

## 1. 交付顺序

1. Web 手动上传到正式 Photo Log/Plog 链路。
2. 服务端异步处理、压缩、Provider 与数据状态机。
3. Android 手动同步。
4. Android 单 MediaStore 文件夹后台最终一致同步。

## 2. 服务端待选项

- API 服务语言、框架和版本策略。
- 数据库及迁移工具；需要支持事务、乐观锁和 JSON/结构化字段。
- Job 队列、调度器、幂等键、重试、取消和恢复机制。
- 压缩图、缩略图、临时原图和导出包的存储布局。
- 内容字节哈希、感知哈希及重复候选算法。
- EXIF、HEIC/HEIF、WebP 和超大图片的安全处理库。
- 搜索实现及可重建索引方案。
- 回收站、永久删除、最小忽略标记和审计记录的数据模型。
- 完整导出 manifest、空白实例导入和一致性校验协议。

## 3. Provider 设计

- 定义版本化统一输入、输出和错误 Schema。
- 实现 Gemini Provider，同时保持供应商可替换。
- 验证支持地区、条款、免费／付费识别和授权状态门禁。
- 固定稳定模型 ID，验证 HIGH 媒体分辨率的质量、成本和延迟。
- 一图一请求的幂等、限流、退避和暂停队列。
- 模型原始响应 30 天清理任务。
- Plog 只使用结构化 Photo Log 文本；设计分批摘要、事实引用和上下文上限。

## 4. Android 待选项

- MediaStore 目标目录标识、递归扫描和 checkpoint 格式。
- 完整／部分／拒绝照片权限状态机。
- 前台发现、内容变化触发、后台任务和周期补偿扫描组合。
- 厂商后台限制检测与应用内说明。
- 持久上传队列、断点续传、指数退避和服务器离线恢复。
- 超大图片受控上传副本及元数据保留方式。
- 永久删除后的忽略来源同步协议。
- Android 与服务端版本号乐观锁交互。

## 5. Web 待选项

- Web UI 技术栈和前后端边界。
- 多文件上传、断点续传和任务状态通道。
- 照片网格虚拟化、按日分组和敏感缩略图模糊。
- Photo Log/Plog 编辑、版本冲突比较和草稿确认。
- 全局设置：默认时区、Plog 语言、每日触发时间、Provider 和同意状态。
- 应用内严重故障横幅，不实现系统通知。

## 6. 部署与网络

- 家庭服务器目标操作系统和硬件资源基线。
- 单进程、分进程或容器化部署方式。
- Tailscale 接入方式；Tailnet 任意设备拥有完整应用权限。
- Gemini Worker 位于官方支持地区时的网络与数据路径。
- TLS、反向代理、密钥注入和配置管理。
- 服务启动、升级、回滚和数据库迁移流程。
- 不提供备份承诺；仍需设计磁盘空间检测和停止接收机制。

## 7. 状态机与一致性验证

- 上传、解析、压缩、识别、整理、取消、删除、恢复的完整状态转换表。
- 删除运行中资产时阻止下游步骤。
- 取消任务后保留部分结果且不自动继续。
- 回收站内容再次同步时不自动复活。
- 恢复时按内容哈希合并并恢复关系。
- Plog 引用删除照片时的匿名占位。
- 一键清空批次、忽略来源和重新导入流程。

## 8. 安全与隐私验证

- 临时原图成功后立即删除，24 小时兜底清理。
- 免费 Provider 风险提示、版本化同意、撤回和条款变化再确认。
- API 密钥不进入客户端、仓库、日志和导出包。
- 模型原始响应和调试数据的访问及 30 天清理。
- 敏感缩略图全局偏好和用户批量覆盖。
- 文件类型、文件头、像素数、解码资源和压缩炸弹防护。

## 9. 可观测性与验收工具

- Job 全链路状态、耗时、错误和重试指标。
- 自动同步发现到建档的 P50/P90/P99。
- 临时原图超期残留硬告警；任何残留阻止 MVP 验收。
- 存储容量阈值与停止上传状态。
- Provider 调用量、限流、失败和队列暂停状态。
- 主题纠正、Plog 确认和错误反馈统计。
- 导出／导入一致性检查和搜索索引重建工具。

## 10. 需要产出的技术文档

- 系统上下文图与组件图。
- API 与事件契约。
- 数据模型和迁移策略。
- Job 状态机及失败恢复设计。
- Android 同步设计。
- Provider 适配层设计。
- 存储、删除与数据迁移设计。
- 部署运行手册和验收测试计划。
